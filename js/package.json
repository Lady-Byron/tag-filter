{
  "name": "lady-byron-tag-filter",
  "private": true,
  "version": "0.1.0",
  "description": "Toolbar tag filter with grouped view when Tag Categories is enabled.",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "clean": "rimraf dist dist-typings ../js/dist || true"
  },
  "devDependencies": {
    "@types/mithril": "^2.0.12",
    "flarum-tsconfig": "^1.0.0",
    "flarum-webpack-config": "^2.0.0",
    "rimraf": "^6.0.1",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.4",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
